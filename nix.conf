experimental-features = nix-command flakes

# Performance optimization (Arch Wiki recommendation)
max-jobs = auto

# Arch Linux locale fix (prevents warnings)
extra-sandbox-paths = /usr/lib/locale/locale-archive
